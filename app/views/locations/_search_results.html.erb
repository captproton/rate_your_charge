<%# app/views/locations/_search_results.html.erb %>
<%= locations.count %>
<% locations.each do |location| %>
  <br>
  <%= link_to locations_path(city: location[0], state: location[1]) do %>
    <%= highlight(location[0], params.dig(:search)) + ",  #{location[1]}" %>
  <% end %>
<% end %>
